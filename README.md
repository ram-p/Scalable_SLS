# Scalable_SLS

This repository contains code for a scalable System Level Synthesis algorithm for optimal control of output-feedback switched systems. The key idea is to synthesize a controller for each possible k-length subword of the switching signal, given the entire language of possible switching signals.
